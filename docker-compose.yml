services:
  db:
    image: mariadb:10.11
    environment:
      MARIADB_ROOT_PASSWORD: secret
      MARIADB_DATABASE: python-de
    ports:
      - "3306:3306"
    volumes:
      - ./data/mariadb:/var/lib/mysql
